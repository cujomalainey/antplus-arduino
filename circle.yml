dependencies:
    pre:
        # Install the latest stable PlatformIO
        - sudo pip install -U platformio
        - platformio platform install teensy

test:
    override:
        - platformio remote test -e teensy31
   
          
